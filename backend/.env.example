# HTTP/WS server port
PORT=8080

# Milliseconds without a GNSS fix before the engine reports DEAD_RECKONING_ONLY
GNSS_OUTAGE_TIMEOUT_MS=3000

# How often (ms) each active session broadcasts a DiagnosticsSnapshot over WebSocket
DIAGNOSTICS_BROADCAST_INTERVAL_MS=200
