# Outage and Replay Engine

The simulation package provides controlled GNSS outage, sensor corruption, replay, and trajectory evaluation boundaries without requiring a physical tunnel drive.

Scenarios live in `scenarios/`. Replay and evaluation implementations can be expanded when field drive logs and reference trajectories are available.
