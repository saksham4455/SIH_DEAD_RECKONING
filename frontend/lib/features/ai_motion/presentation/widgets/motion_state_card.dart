import 'package:flutter/material.dart';

class MotionStateCard extends StatelessWidget {
  const MotionStateCard({super.key});

  @override
  Widget build(BuildContext context) =>
      const Card(child: ListTile(title: Text('AI motion state')));
}
