import 'package:flutter/material.dart';

class GnssSignalIndicator extends StatelessWidget {
  const GnssSignalIndicator({super.key});

  @override
  Widget build(BuildContext context) => const Icon(Icons.gps_fixed);
}
