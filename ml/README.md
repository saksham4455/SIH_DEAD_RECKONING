# ML Training Pipeline

This directory contains the architecture for training the speed estimator and vibration classifier from field sensor datasets.

The current files define module boundaries and configuration paths. Full PyTorch training, ONNX conversion, and INT8 TFLite quantization are intentionally pending model and dataset integration.
